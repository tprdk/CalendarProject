<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/item_search"
        android:icon="@drawable/ic_search_events"
        android:title="Searching events"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/item_add"
        android:icon="@drawable/ic_add_event"
        android:title="Adding events"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/item_settings"
        android:icon="@drawable/ic_settings"
        android:title="Settings"
        app:showAsAction="ifRoom"/>

</menu>